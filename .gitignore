# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# We commit Cargo.lock for the eventual binary app; keep it for reproducible
# builds (golden-frame determinism, ORCHESTRATION.md §9). Remove this note if
# the workspace is ever published purely as libraries.

# SvelteKit / node (chroma-ui, added at M4)
node_modules/
/app/build
/app/.svelte-kit
/app/dist

# Tauri (chroma-tauri, added at M3+)
/app/src-tauri/target

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Local scratch
/scratch
*.local
