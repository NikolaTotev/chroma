{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability: lets the main window call Chroma Studio's commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
